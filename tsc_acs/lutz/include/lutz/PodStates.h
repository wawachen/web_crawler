/*H
Copyright 2017 Transport Systems Catapult (The Catapult)
All rights reserved
For use only for the purpose agreed with The Catapult
H*/

#pragma once

namespace tsc_acs {
namespace lutz {

enum PodStateId
{
	INVALID        = 0,
	KEY_OFF		   = 1,
	MANUAL         = 2,
	ERROR          = 3,
	E_STOP         = 4,
	NO_SESSION     = 5,
	NEW_SESSION    = 6,
	REQ_CENTRE     = 7,
	CHECK_CENTER   = 8,
	PRIMED         = 9,
	AUTO_DELAY     = 10,
	AUTONOMOUS     = 11,
	OVERRIDE       = 12,
	CHARGING       = 13,

	POD_STATE_COUNT
};

const char *const PodState[] =
{
	"INVALID",      // = 0,
	"KEY_OFF",		// = 1,
	"MANUAL",       // = 2,
	"ERROR",        // = 3,
	"E_STOP",       // = 4,
	"NO_SESSION",   // = 5,
	"NEW_SESSION",  // = 6,
	"REQ_CENTRE",   // = 7,
	"CHECK_CENTER", // = 8,
	"PRIMED",       // = 9,
	"AUTO_DELAY",   // = 10,
	"AUTONOMOUS",  	// = 11,
	"OVERRIDE",    	// = 12,
	"CHARGING",  	// = 13,
};

enum BrakePedalForce
{
	BRAKE_PEDAL_FORCE_UPPER = 210,
	BRAKE_PEDAL_FORCE_LOWER = 200,
};

enum Seats
{
	EMPTY,
	OCCUPIED,
	OCCUPIED_BELTED,
};

enum ReadyState
{
	ZERO_STATE 						= 0x0,
	//POD READY if
	POD_READY 						= 0x1,
	DRIVER_SEAT_ERROR 				= 0x2,
	PASSENGER_SEAT_ERROR 			= 0x4,
	POD_DOOR_ERROR 					= 0x8,
	CHARGE_DOOR_ERROR 				= 0x10,

	//STATIONARY if
	POD_STATIONARY 					= 0x20,
	POD_STATIONARY_ERROR 			= 0x40,
	PARK_BRAKE_ON_ERROR 			= 0x80,

	//HANDSHAKE GOOD if
	HANDSHAKE_GOOD 					= 0x100,
	QOS_ERROR 						= 0x200,

	//AUTONOMY READY if
	AUTONOMY_READY 					= 0x400,
	GEAR_FWD_ERROR 					= 0x800,
	THROTTLE_ERROR 					= 0x1000,
	//	STATIONARY
	//	POD_READY

	//SESSION VALID if
	SESSION_VALID					= 0x2000,
	SESSION_ID_ERROR				= 0x4000,
	STATE_REQ_ERROR 				= 0x8000,
	//	AUTONOMY_READY
	APPLY_BRAKE_ERROR				= 0x10000,

	//PRIMED VALID if
	PRIMED_VALID					= 0x20000,
	//	STATE_REQ_ERROR
	//	AUTONOMY_READY
	STEERING_CENTRING_ERROR			= 0x40000,

	//AUTONOMOUS VALID IF
	AUTONOMOUS_VALID				= 0x80000,
	//	STATE_REQ_ERROR
	//	AUTONOMY_READY
	CTRL_CMD1_ERROR					= 0x100000,
	CTRL_CMD1_DIR_ERROR				= 0x200000,
	CTRL_CMD1_MAX_SPEED_ERROR		= 0x400000,
	CTRL_CMD1_SPEED_MODE_ERROR		= 0x800000,
	CTRL_CMD1_TORQUE_LIMIT_ERROR	= 0x1000000,
	CTRL_CMD1_PARK_BRAKE_ERROR		= 0x2000000,
	CTRL_CMD1_THROTTLE_POT_ERROR	= 0x4000000,

	//SESSION CANCELLED TRUE if
	SESSION_CANCELLED				= 0x8000000,
	//	STATIONARY
	//	STATE_REQ_ERROR
	GEAR_NOT_IN_FWD_ERROR			= 0x10000000,
	HANDSHAKE_SESSION_ID_ERROR		= 0x20000000,
	STEERING_ERROR					= 0x40000000,
	MANUAL_THROTTLE_ERROR			= 0x80000000,

	//REVOKE TRUE if
	REVOKE_TRUE						= 0x100000000,
	MANUAL_STEERING_ERROR			= 0x200000000,
	MANUAL_BRAKE_ERROR				= 0x400000000,
	MANUAL_GEAR_ERROR				= 0x800000000,
	//	MANUAL_THROTTLE_ERROR

	//OVERRIDE TRUE if
	OVERRIDE_TRUE					= 0x1000000000,
	//	POD_NOT_READY
	// HANDSHAKE_NOT_GOOD
	STATE_REQ_NO_SESSION_ERROR		= 0x2000000000,
	VEHICLE_SPEED_ERROR				= 0x4000000000,
	PARK_BRAKE_OFF_ERROR			= 0x8000000000,

	//ERROR TRUE if
	ERROR_TRUE 						= 0x10000000000,

	//E-STOP TRUE if
	E_STOP_TRUE						= 0x20000000000,
	E_STOP_BUTTON_ERROR				= 0x40000000000,
	FRONT_BUMPER_ERROR				= 0x80000000000,
	REAR_BUMPER_ERROR				= 0x100000000000,
};

}
}
